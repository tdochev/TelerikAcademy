var width = [3, 2.5, 5],
	height = [4, 3, 5],
	i,
	lenght;

for (i = 0; i < width.length; i += 1) {
	console.log('widht = ' + width[i] + ', height = ' + height[i] + ', area = ' + width[i] * height[i]);
}